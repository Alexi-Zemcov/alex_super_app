interface:
  display_name: "Module Integration"
  short_description: "Add a module entry point and wire it to a host app."
  default_prompt: "Use $module-integration to add this module cleanly, expose only its public integration contract, and wire it into the consuming app without leaking src APIs."
policy:
  allow_implicit_invocation: true
